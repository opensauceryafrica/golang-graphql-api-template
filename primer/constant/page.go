package constant

const (
	// PageLimit is the default limit for pagination
	PageLimit = 10

	// PageOffset is the default offset for pagination
	PageOffset = 0

	// PageSort is the default sort order for pagination (desc)
	PageSort = "desc"
)
